// The browser windows is called as a window and inside that window there's a console where we can log something and show it in the browser
// window.console.log Here, we don't need to write window because by default all javascript is in the window element
// console.log(testing);